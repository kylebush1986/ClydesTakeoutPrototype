@model ClydesTakeoutPrototype.Models.OrderModels.Order

@{
    ViewData["Title"] = "Index";
}


<h1>Orders</h1>

<div>
    <h4>Order</h4>
    <hr />
    <dl class="row">
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.ID)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.ID)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.PickupTime)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.PickupTime)
        </dd>
        <dt class="col-sm-2">
            @Html.DisplayNameFor(model => model.Total)
        </dt>
        <dd class="col-sm-10">
            @Html.DisplayFor(model => model.Total)
        </dd>
    </dl>
</div>
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>

@*@model List<ClydesTakeoutPrototype.Models.OrderModels.Order>

@{
    ViewData["Title"] = "Employee View";
}

<h1>Employee View</h1>

@foreach (var order in Model)
{
    <div>
        <h4>@order.ID</h4>
        <hr />
        @foreach (var item in order.Items)
        {
            <div>
                <div>
                    <h4>@item.Name</h4>
                </div>
                <div>
                    <p>@order.PickupTime</p>
                </div>
            </div>
        }
    </div>
}
<div>
    @Html.ActionLink("Edit", "Edit", new { /* id = Model.PrimaryKey */ }) |
    <a asp-action="Index">Back to List</a>
</div>*@
